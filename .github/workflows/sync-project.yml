name: sync-project

on:
  schedule:
    - cron: "17 6 * * *"
  workflow_dispatch:

permissions: {}

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: scripts/sync-project.sh
        env:
          GH_TOKEN: ${{ secrets.PROJECT_TOKEN }}
